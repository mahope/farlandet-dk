# Nixpacks configuration for Farlandet.dk
providers = ["node"]

[variables]
NODE_ENV = "production"

[phases.install]
cmds = ["npm ci", "npm install -g serve"]

[phases.build]
cmds = ["npm run build:prod"]

[start]
cmd = "serve -s dist -l ${PORT:-3000}"