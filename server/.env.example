# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (server/.env)
# =============================================================================
# Copy this file to server/.env and update the values for your environment

# =============================================================================
# Database Configuration
# =============================================================================
# Individual database connection parameters (for development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=farlandet
DB_USER=postgres
DB_PASSWORD=your_password_here

# Production database connection string (preferred for production)
# Use this format for Railway, Render, or other hosting providers:
# DATABASE_URL=postgresql://username:password@hostname:port/database
# Example: postgresql://postgres:mypassword@containers-us-west-123.railway.app:5432/railway

# For Railway PostgreSQL (uncomment and replace with your values):
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# For Render PostgreSQL (uncomment and replace with your values):
# DATABASE_URL=postgresql://user:pass@hostname:port/dbname

# =============================================================================
# Server Configuration
# =============================================================================
# Port for the backend server (Railway uses $PORT automatically)
PORT=3001

# Environment mode
NODE_ENV=development

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:5173

# =============================================================================
# Authentication & Security
# =============================================================================
# JWT Secret Key (MUST be changed for production!)
# Generate a secure key: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_change_in_production

# JWT Token expiration (default: 24h)
JWT_EXPIRES_IN=24h

# CORS allowed origins (frontend URL)
CORS_ORIGIN=http://localhost:5173

# API Base URL (used by better-auth library)
API_BASE_URL=http://localhost:3001

# =============================================================================
# Logging & Monitoring
# =============================================================================
# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable request logging
LOG_REQUESTS=true

# =============================================================================
# PRODUCTION DEPLOYMENT SETTINGS
# =============================================================================
# For Railway deployment:
# NODE_ENV=production
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# FRONTEND_URL=https://your-frontend-domain.com
# CORS_ORIGIN=https://your-frontend-domain.com
# JWT_SECRET=your-secure-production-jwt-secret
# API_BASE_URL=https://your-backend-domain.com
# PORT=$PORT (Railway sets this automatically)
# LOG_LEVEL=warn

# For Render deployment:
# NODE_ENV=production
# DATABASE_URL=postgresql://user:pass@hostname:port/dbname
# FRONTEND_URL=https://your-app.onrender.com
# CORS_ORIGIN=https://your-app.onrender.com
# JWT_SECRET=your-secure-production-jwt-secret
# API_BASE_URL=https://your-app.onrender.com
# PORT=$PORT (Render sets this automatically)
# LOG_LEVEL=warn

# =============================================================================
# SECURITY NOTES FOR PRODUCTION:
# =============================================================================
# 1. Always use a strong JWT_SECRET (32+ random characters)
# 2. Set NODE_ENV=production
# 3. Use DATABASE_URL instead of individual DB_* variables
# 4. Set CORS_ORIGIN to your exact frontend domain
# 5. Consider setting LOG_LEVEL=warn or LOG_LEVEL=error
# 6. Never commit .env files to version control
# 7. Use environment variables in your hosting platform